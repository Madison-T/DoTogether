import React from 'react';
import { View, Text } from 'react-native';

const NextScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ fontSize: 24 }}>Next Screen</Text>
    </View>
  );
};

export default NextScreen;
